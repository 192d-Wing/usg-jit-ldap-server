[package]
name = "usg-jit-ldap-server"
version = "0.1.0"
edition = "2021"
description = "Minimal, security-focused LDAP server for JIT password broker environments"

[dependencies]
bytes = "1"
thiserror = "1"
tracing = "0.1"
tokio = { version = "1", features = ["full"] }
