// USG JIT LDAP Server — Entry point stub
// Actual TLS listener and service wiring provided by the Runtime agent.

mod ldap;

fn main() {
    println!("USG JIT LDAP Server — use `cargo check` to validate protocol module");
}
